<%= form_for @task, url: task_path(id: @task.id), method: :patch do |f| %>

	<div class="container">
		<div><h1>Your <strong>Goals</strong> </h1></div>
		<div><%= f.text_area :description%></div>
		<div><%= f.submit "Submit" %></div>
		
	</div>


<% end %>